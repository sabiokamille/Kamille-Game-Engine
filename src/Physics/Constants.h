#ifndef CONSTANTS_H
#define CONSTANTS_H

const int FPS = 60; // app running at 60 frames per second
const int MILLISECS_PER_FRAME = 1000 / FPS; //each frame should take 16.6 milliseconds to run
const int PIXELS_PER_METER = 50;
#endif